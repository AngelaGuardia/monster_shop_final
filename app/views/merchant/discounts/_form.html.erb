<%= form_for [@merchant, @discount] do |f| %>
  <%= f.label :Name %>
  <%= f.text_field :name %><br>

  <%= f.label :Percentage %>
  <%= f.text_field :percentage %><br>


  <%= f.label :minimum_quantity %>
  <%= f.number_field :minimum_quantity %><br>

  <%= f.submit %>
<% end %>
